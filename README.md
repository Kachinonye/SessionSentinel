# Session Monitor

A professional Bash script for tracking user activity on a Linux system.  
It logs:
- Active user sessions
- SSH connections
- Recent logins
- Recent sudo usage
- Alerts when specific users (e.g. root) are logged in

## Usage

```bash
chmod +x monitor-session.sh
./monitor-session.sh

